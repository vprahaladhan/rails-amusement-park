<h1>Listing Attractions</h1>

<div>Name : Tickets : Details</div>

<ul>
  <% @attractions.each do |a| %>
    <li><%= a.name + " : " + a.tickets.to_s + " : " %><%= link_to "Go on " + a.name, attraction_path(a.id) %></li>
  <% end %>
</ul>